/* eslint-env browser */

(function(context) {
  "use strict";

  function getContentAsJSON(url) {
    // request url, return promise that resolves the response as a json object
  }

  context.getJSON = getContentAsJSON;

}(window));